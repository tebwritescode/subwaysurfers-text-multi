# Core Flask dependencies
Flask==3.0.3
Werkzeug==3.0.2
Jinja2==3.1.3
itsdangerous==2.1.2
click==8.1.7
blinker==1.7.0
MarkupSafe==2.1.5

# Web and HTTP
requests==2.31.0
urllib3==2.2.1
certifi==2024.2.2
charset-normalizer==3.3.2
idna==3.6
validators==0.34.0

# Text processing and parsing
beautifulsoup4==4.12.3
goose3==3.1.19
cssselect==1.2.0
soupsieve==2.5
langdetect==1.0.9

# Video and image processing
opencv-python==4.9.0.80
pillow==11.3.0
moviepy==1.0.3
imageio==2.34.0
imageio-ffmpeg==0.4.9
proglog==0.1.10

# Audio processing
audioread==3.0.1
pydub==0.25.1

# Utilities
numpy==1.26.4
tqdm==4.66.2
six==1.16.0
python-dateutil==2.9.0.post0
decorator==4.4.2

# Optional dependencies (with fallbacks in code)
srt==3.5.3

# Skip problematic packages that have fallbacks
# cffi - has ffmpeg fallback
# lxml - has alternative parsing fallback  
# vosk - not needed with WhisperASR
# playsound - not used in Docker
# pyahocorasick - optional optimization
# pycparser - cffi dependency
# websockets - not needed