# Web framework
Flask==3.0.3
Werkzeug==3.0.6
Jinja2==3.1.3
MarkupSafe==2.1.5
click==8.1.7
itsdangerous==2.1.2
blinker==1.7.0

# Configuration (.env support)
python-dotenv==1.0.1

# HTTP + input validation (used by every TTS backend and URL handling)
requests==2.31.0
validators==0.34.0

# Audio handling: concatenate TTS chunks and convert mp3 -> wav.
# audioop-lts backfills the stdlib `audioop` module removed in Python 3.13.
pydub==0.25.1
audioop-lts==0.2.2

# Article extraction from URLs
goose3==3.1.19
lxml==6.1.0
beautifulsoup4==4.12.3
cssselect==1.2.0
soupsieve==2.5
python-dateutil==2.9.0.post0

# NOTE: video composition and caption burning are done by invoking the system
# `ffmpeg` binary (installed in the Docker image), so no Python video libraries
# (moviepy / opencv / imageio) are required.
